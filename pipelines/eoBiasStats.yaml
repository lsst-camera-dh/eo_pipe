description: eo_pipe task to gather bias frame statistics
tasks:
  bias_stats:
    class: lsst.eo.pipe.BiasStatsTask
    config:
      connections.raw_frames: 'raw'
      connections.bias_stats: 'eoBiasStats'
      edge_buffer: 2
