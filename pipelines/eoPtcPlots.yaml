description: eo_pipe task to plot PTC results for the LSSTCam focal plane
tasks:
  ptc_plots:
    class: lsst.eo.pipe.PtcPlotsTask
    config:
      connections.ptc_results: "PhotonTransferCurveDataset"
      connections.ptc_plots: "eoPtcPlots"
      figsize: (12, 10)
