description: Pipeline to gather bias frame statistics.
tasks:
  readNoise:
    class: lsst.eo.pipe.ReadNoiseTask
    config:
      connections.raw_frames: 'raw'
      connections.read_noise: 'eoReadNoise'
      edge_buffer: 2
