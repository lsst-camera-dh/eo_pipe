includeConfigs:
  - ${PWD}/bps/bps_butler_config.yaml
  - ${PWD}/bps/cp_pipe/bps_qgraph_options.yaml

isrTaskName: cpDarkIsr
extraQgraphOptions: "{isr_QgraphOptions}"

pipelineYaml: "$CP_PIPE_DIR/pipelines/${INSTRUMENT_NAME}/cpDark.yaml#cpDarkIsr,cpDark,cpDarkCombine"
instrument: ${INSTRUMENT_CLASS}

payload:
  day_obs: ${DAYOBS}
  seq_num_range: ${SEQNUM_RANGE}
  weekly: ${WEEKLY}
  payload_modifier: ${PAYLOAD_MODIFIER}
  inCollection: ${INCOLLECTION}
  payloadName: dark_{day_obs}_{seq_num_range}_{weekly}
  butlerConfig: ${BUTLER_CONFIG}
  dataQuery: "instrument='${INSTRUMENT_NAME}' and day_obs={day_obs} and seq_num in ({seq_num_range}) and exposure.observation_type='dark' ${DETECTOR_SELECTION}"
