pipelineYaml: ${EO_PIPE_DIR}/pipelines/eoRaftMosaics.yaml

payload:
  b_protocol_run: 13162
  weekly: ${WEEKLY}
  inCollection: u/jchiang/flat_{b_protocol_run}_{weekly}
  payloadName: raft_mosaics_{b_protocol_run}_{weekly}
  butlerConfig: ${BUTLER_CONFIG}
  dataQuery: "instrument='LSSTCam' and exposure.science_program='{b_protocol_run}' and exposure.observation_type='flat' and exposure.observation_reason='lambda'"

includeConfigs:
  - ${CP_PIPE_PROCESSING_DIR}/bps/ctrl_bps_parsl_config.yaml

computeSite: slurm_wq_32
