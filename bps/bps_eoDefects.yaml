pipelineYaml: "${EO_PIPE_DIR}/pipelines/eoDefects.yaml"

instrument: lsst.obs.lsst.LsstCam

payload:
  b_protocol_run: 13162
  weekly: ${WEEKLY}
  inCollection: u/{operator}/defects_{b_protocol_run}_w_2022_39
  payloadName: eo_pipe_defects_{b_protocol_run}_{weekly}
  butlerConfig: ${BUTLER_CONFIG}
  dataQuery: "instrument='LSSTCam' and detector in (15,90..98,171)"

includeConfigs:
  - ${CP_PIPE_PROCESSING_DIR}/bps/ctrl_bps_parsl_config.yaml

computeSite: slurm_wq_16
