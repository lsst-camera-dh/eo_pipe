includeConfigs:
  - ${PWD}/bps/bps_butler_config.yaml

plot_label: ${DATASET_LABEL}

pipelineYaml: ${EO_PIPE_DIR}/pipelines/eoCtiVsFlux.yaml
extraQgraphOptions: "--config ctiVsFluxTask:acq_run=' {plot_label}' --config ctiVsFluxTask:do_parallel_oscan=${DO_PARALLEL_OSCAN}"

instrument: ${INSTRUMENT_CLASS}

payload:
  dataset_selection: ${DATASET_SELECTION}
  dataset_label: ${DATASET_LABEL}
  weekly: ${WEEKLY}
  payload_modifier: ${PAYLOAD_MODIFIER}
  inCollection: ${EO_PIPE_INCOLLECTION}
  payloadName: eo_cti_vs_flux{payload_modifier}_{dataset_label}_{weekly}
  butlerConfig: ${BUTLER_CONFIG}
  dataQuery: "instrument='${INSTRUMENT_NAME}' and {dataset_selection} and exposure.observation_type='flat' and exposure.observation_reason='ptc' ${DETECTOR_SELECTION}"
