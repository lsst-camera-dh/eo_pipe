pipelineYaml: ${EO_PIPE_DIR}/pipelines/eoCtiVsFlux.yaml

wmsServiceClass: lsst.ctrl.bps.parsl.ParslService

payload:
  b_protocol_run: 13162
  weekly: ${WEEKLY}
  inCollection: u/jchiang/flat_13162_w_2022_53
  payloadName: cti_vs_flux_{b_protocol_run}_{weekly}
  butlerConfig: ${BUTLER_CONFIG}
  dataQuery: "instrument='LSSTCam' and exposure.science_program='{b_protocol_run}' and exposure.observation_type='flat' and exposure.observation_reason='flat'"

includeConfigs:
  - ${CP_PIPE_PROCESSING_DIR}/bps/ctrl_bps_parsl_config.yaml

computeSite: slurm_wq_16
