includeConfigs:
  - ${PWD}/bps/bps_butler_config.yaml

plot_label: "${DAYOBS}, ${SEQNUM_RANGE}, no pd data"

pipelineYaml: ${EO_PIPE_DIR}/pipelines/eoFlatGainStability.yaml
extraQgraphOptions: "--config flatGainStabilityPlots:acq_run=' {plot_label}' --config flatGainStabilityTask:do_parallel_oscan=${DO_PARALLEL_OSCAN} --config flatGainStabilityTask:do_apply_pd_normalization=False"

instrument: ${INSTRUMENT_CLASS}

payload:
  day_obs: ${DAYOBS}
  seq_num_range: ${SEQNUM_RANGE}
  weekly: ${WEEKLY}
  payload_modifier: ${PAYLOAD_MODIFIER}
  inCollection: ${INCOLLECTION}
  payloadName: eo_flat_gain_stability{payload_modifier}_{day_obs}_{seq_num_range}_{weekly}
  butlerConfig: ${BUTLER_CONFIG}
  dataQuery: "instrument='${INSTRUMENT_NAME}' and exposure.observation_type='flat' and day_obs={day_obs} and exposure.seq_num in ({seq_num_range}) ${DETECTOR_SELECTION}"
